apiVersion: apps/v1
kind: Deployment
metadata:
  name: kibana
  namespace: observability-demo
spec:
  template:
    spec:
      containers:
        - name: kibana
          env:
            - name: ELASTICSEARCH_HOSTS
              value: http://elasticsearch-wrong:9200
