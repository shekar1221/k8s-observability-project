apiVersion: apps/v1
kind: Deployment
metadata:
  name: orders-api
  namespace: observability-demo
spec:
  template:
    spec:
      containers:
        - name: orders-api
          image: orders-api:missing-lab-image
          imagePullPolicy: Always
